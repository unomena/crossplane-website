import React from 'react';
import PageProvider from 'src-new/components/PageProvider';

type Props = {};

const Customers = (props: Props) => {
  return <PageProvider displayTitle="Customers">Customers</PageProvider>;
};

export default Customers;
